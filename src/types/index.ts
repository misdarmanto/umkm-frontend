export interface MenuTypes {
	id: number;
	title: string;
	description: string;
	image: string;
	price: number;
	stars: number;
}

export interface FeedbackTypes {
	id: number;
	name: string;
	message: string;
	image: string;
}

export interface ChefTypes {
	id: number;
	name: string;
	image: string;
}
